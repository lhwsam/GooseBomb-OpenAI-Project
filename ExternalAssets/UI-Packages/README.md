# 로컬 UI 패키지 보관함

다운로드한 `.unitypackage`, `.zip`, 원본 PSD 등 Unity에 아직 import하지 않은 공급자 파일을 임시 보관하는 위치다.

이 폴더의 다운로드 파일은 저장소 용량과 라이선스 문제를 피하기 위해 Git에서 제외한다. 실제로 사용할 패키지는 Unity에서 검토해 import하고, 가능한 경우 `Assets/ThirdParty/UI/<PackageName>` 아래에 원본 상태로 둔다.
